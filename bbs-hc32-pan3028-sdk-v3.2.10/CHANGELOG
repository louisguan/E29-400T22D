版本：SDK_V3.2.10
日期：2022/11/11
更新内容：
1、修改RSSI计算方法。
2、修改TXPOWER范围，增加一个功率挡位。
3、增加LNA高低增益切换。

版本：SDK_V3.2.9
日期：2022/09/21
更新内容：
1、修改寄存器P032，优化性能。

版本：SDK_V3.2.8
日期：2022/09/20
更新内容：
1、修改初始化配置，重新验证发射波形。
2、修改OLED工程发射模式为singleTX。
3、增加各类认证的相关配置，具体在radio.h下。

版本：SDK_V3.2.7
日期：2022/06/29
更新内容：
1、修改灵敏度阈值，默认0x07，CAD检测0x10，自搜索0x0f。
2、增加CAD关闭接口函数，关闭时恢复灵敏度阈值。
3、修改自搜索接口函数，默认搜索范围SF7~9，关闭自搜索时恢复preamble长度。
4、调整文件包含，头文件集中到port.h下。
5、更新硬件参考设计
6、增加433M、915M认证报告

版本：SDK_V3.2.6
日期：2022/05
更新内容：
1、修改CR默认值，与计算器一致。
2、修改RF中断处理逻辑，采用中断标志+事件轮询。


版本：SDK_V3.2.5
日期：2022/05/06
更新内容：
1、修复单次超时接收之后的复位逻辑。
2、修改deepsleep_wakeup函数，修复部分寄存器不能复位的问题。
3、修改频率计算方式，修改为整形运算。
4、修改清中断函数中的延时逻辑。

版本：SDK_V3.2.4
日期：2022/03/30
更新内容：
1、修复V3.2.3版本自搜索设置前导码接口函数PAN3028_set_all_sf_preamble返回值错误问题。
2、调整发射功率支持范围（现在修改为0-29共30个挡位），调整发射功率函数。

版本：SDK_V3.2.3
日期：2022/02/11
更新内容：
1、修复V3.2.2版本收发互通异常BUG。
2、修改频率计算方式，采用fb、fc计算，支持范围360-594MHz，720-1180MHz。

版本：SDK_V3.2.2
日期：2022/01/25
更新内容：
1、单次超时接收超时后，修改处理逻辑，解决该情况下模块电流异常问题。
2、修改PAN3028_clr_irq清中断函数，增加返回值，和多次清中断操作。减小RXERR后又出现RXDONE中断问题的概率。
3、修改频率规划范围，支持360-600MHz，720-1200MHz

版本：SDK_V3.2.1
日期：2021/11/12
更新内容：
1、增加智能搜索功能，增加了智能搜索打开/关闭，智能搜索前导码配置三个接口函数。
2、文档修改，修改了SDK测试结果，增加了V3.2.1版本测试记录。
3、新增了模块及EVB板的设计资料
4、新增PAN3028_SDK清单
5、更新了DOC下文档格式


版本：SDK_V3.1.1
日期：2021/11/5
更新内容：
1、代码修改
（1）初始化增加了PAN3028_write_spec_page_reg(PAGE0_SEL,0x4d,0x0e)寄存器配置。在某些异常情况下，该寄存器值可能异常，故增加该寄存器配置。
（2）修改了PAN3028_deepsleep_wakeup，PAN3028_sleep_wakeup，PAN3028_deepsleep，PAN3028_sleep几个函数内的延时时间。用以解决部分芯片初始化后，芯片收发异常的BUG。
（3)修改了AGC初始化部分的寄存器读写逻辑，减小芯片的初始化耗时。
2. 其他说明
（1）目前SDK 采用的SPI速率为1.5625MHz。目前测试rf_init时间在9ms以内，
（2）之前发现的一致性高低温测试中出现的RX的PLL失锁问题，该版本已修复，修改位置为：寄存器PAGE0.0X4A和寄存器PAGE0.0X4C。

版本：SDK_V3.0.1
日期：2021/9/9
更新内容：
1、Release新版本初始内容
